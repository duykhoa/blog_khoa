.article-container
  .feature-image
    = image_tag article.feature_image.url(:medium)
  .post-time
    = format_date(article.created_at)
  .title
    = article.title
  .short-content
    = article.short_content
- if article_counter % 2 != 0
  .clear
